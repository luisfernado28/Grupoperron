<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.luisfernando.superhipermegaperron">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Product_Activity" />
        <activity android:name=".Computer_Activity" />
        <activity android:name=".Registrarse" />
        <activity android:name=".MenuActivity" />
        <activity android:name=".TVActivity" />
        <activity android:name=".TelefonoActivity" />
        <activity android:name=".JuegosActivity" />
        <activity android:name=".ElectroActivity" />
        <activity android:name=".MusicaActivity" />
        <activity android:name=".Wish_List"></activity>
    </application>

</manifest>